# Prompt Registry for Agent1 Pipeline
# Maps prompt names to their versions and file paths

prompts:
  schema_discovery:
    current_version: "v1.0"
    versions:
      v1.0:
        file: "schema_discovery/v1.0.yaml"
        description: "Schema discovery prompt for GRC document analysis"

  requirement_atomizer:
    current_version: "v1.0"
    versions:
      v1.0:
        file: "requirement_atomizer/v1.0.yaml"
        description: "Base requirement extraction prompt"
      v1.0_retry:
        file: "requirement_atomizer/v1.0_retry.yaml"
        description: "Conservative retry prompt with higher confidence threshold"

  grc_extractor:
    current_version: "v1.0"
    versions:
      v1.0:
        file: "grc_extractor/v1.0.yaml"
        description: "GRC component extraction from table chunks (ported from rule_extraction v1.2)"
